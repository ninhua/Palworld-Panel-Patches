# PalPanel patch tracks

Active track: `candidate-v1.3.0`.

The complete package contains no legacy dev track. Failed migration workspaces are persisted only on `migration/vX.Y.Z` branches; verified stable workspaces are persisted as `stable-vX.Y.Z`.
